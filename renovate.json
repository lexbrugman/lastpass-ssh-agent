{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "baseBranchPatterns": [
    "dev"
  ],
  "dependencyDashboard": false,
  "pinDigests": false,
  "lockFileMaintenance": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "One exception, not three: these are all pinned by the same chain. ssh-agent-lib 0.6 depends on ssh-key ^0.6, and ssh-key 0.6 is built on signature 2, rand_core 0.6 and rsa 0.9. We declare signature and rand_core directly only to import the traits ssh-key and rsa implement, and to guarantee the getrandom feature OsRng needs (see Cargo.toml) — so their versions are not ours to choose. Bumping one alone puts two incompatible copies of a crate in one binary, and cargo either fails to resolve or compiles the wrong traits; neither is fixable on this side. Lift all three together, and only once ssh-agent-lib publishes a release built on ssh-key 0.7+ — that is what unblocks the RustCrypto next generation (signature 3, rand_core 0.9+, rsa 0.10), which is still in release candidate. rsa 0.10 is also what would retire the RUSTSEC-2023-0071 exception in .cargo/audit.toml, so there is a real reason to revisit rather than let this sit. Patch updates are unaffected, and the caret ranges already absorb anything compatible through lockFileMaintenance.",
      "matchManagers": [
        "cargo"
      ],
      "matchPackageNames": [
        "ssh-key",
        "signature",
        "rand_core"
      ],
      "matchUpdateTypes": [
        "major",
        "minor"
      ],
      "enabled": false
    },
    {
      "description": "These two are one decision, for the same reason as the block above but with a quieter failure. security-framework 3 requires core-foundation ^0.10, and we declare core-foundation directly only because platform.rs reads one key out of the dictionary CGSessionCopyCurrentDictionary returns. Either of them moving alone resolves two copies of core-foundation into one binary — us taking 0.11 first, or a future security-framework requiring 0.11 while our direct pin stays at 0.10. Both compile today, because platform.rs and keychain.rs never hand each other a CoreFoundation value and so the duplicate types never meet; that silence is exactly why this needs a rule rather than a failing build to catch it. Lift the pair together. Patch updates are unaffected, and the caret ranges already absorb anything compatible through lockFileMaintenance.",
      "matchManagers": [
        "cargo"
      ],
      "matchPackageNames": [
        "core-foundation",
        "security-framework"
      ],
      "matchUpdateTypes": [
        "major",
        "minor"
      ],
      "enabled": false
    },
    {
      "description": "The dev image's base tag floats within a major (rust:1-slim picks up new minors on 'docker compose build --pull', matching how CI floats on dtolnay/rust-toolchain@stable), so only the major needs a pull request. Grouping it keeps the Dockerfile and compose file in one change.",
      "matchManagers": [
        "dockerfile",
        "docker-compose"
      ],
      "groupName": "dev container base image"
    },
    {
      "description": "compose.yaml names the locally built dev image so all four services share one build. It is not published anywhere, so there is no registry for Renovate to look it up in.",
      "matchManagers": [
        "docker-compose"
      ],
      "matchPackageNames": [
        "lastpass-ssh-agent-dev"
      ],
      "enabled": false
    }
  ]
}
